// module.exports.recordToAgoraFormat = function(record) {
//   const timeTaken = Math.round(record.timeTaken);
//   return `"${record.provider}" ${record.httpMethod} ${record.statusCode} ${record.url} ${timeTaken} ${record.responseSize} ${record.cacheStatus}`;
// }

// module.exports.